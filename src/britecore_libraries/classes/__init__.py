"""Removed compatibility import path for classes module."""

raise ImportError(
    "britecore_libraries.classes has been removed. Import from "
    "britecore_libraries.models and britecore_libraries.validators."
)
