"""BriteCore V2 authority_limits API endpoint wrappers."""

from __future__ import annotations

from typing import Any, Unpack

from britecore_sdk.api.api_calls import (
    BritecoreAPIClient,
    RequestParameters,
    api_client,
)

API_CLIENT: BritecoreAPIClient = api_client


def _coerce_authority_limit_payload(authority_limit: Any | None) -> Any | None:
    """Accept model instances and convert them to request-safe dict payloads."""
    if authority_limit is None:
        return None
    if hasattr(authority_limit, "to_dict") and callable(authority_limit.to_dict):
        return authority_limit.to_dict()
    return authority_limit


def _coerce_authority_limits_payload(authority_limits: Any | None) -> Any | None:
    """Coerce sequence payloads of authority limits into request-safe dicts."""
    if authority_limits is None:
        return None
    if isinstance(authority_limits, list):
        return [_coerce_authority_limit_payload(item) for item in authority_limits]
    return authority_limits


# --- Autogenerated spec wrappers ---


def create_authority_limit(
    authority_limit: Any | None = None,
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """Create a new authority limit.

    POST /api/v2/authority_limits/create_authority_limit
    """
    request_json: dict[str, Any] = {
        "authority_limit": _coerce_authority_limit_payload(authority_limit)
    }
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/authority_limits/create_authority_limit",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/authority_limits/create_authority_limit"
    )


def delete_authority_limit(
    id: Any | None = None,
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """Delete an authority limit by identifier.

    POST /api/v2/authority_limits/delete_authority_limit
    """
    request_json: dict[str, Any] = {"id": id}
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/authority_limits/delete_authority_limit",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/authority_limits/delete_authority_limit"
    )


def get_authority_limit(
    id: Any | None = None,
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """Get Authority Limit.

    POST /api/v2/authority_limits/get_authority_limit
    """
    request_json: dict[str, Any] = {"id": id}
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/authority_limits/get_authority_limit",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/authority_limits/get_authority_limit"
    )


def list_authority_limits(
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """Retrieve all authority limits.

    POST /api/v2/authority_limits/list_authority_limits
    """
    request_json: dict[str, Any] = {}
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/authority_limits/list_authority_limits",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/authority_limits/list_authority_limits"
    )


def update_all_authority_limits(
    authority_limits: Any | None = None,
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """Update All Authority Limits.

    POST /api/v2/authority_limits/update_all_authority_limits
    """
    request_json: dict[str, Any] = {
        "authority_limits": _coerce_authority_limits_payload(authority_limits)
    }
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/authority_limits/update_all_authority_limits",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/authority_limits/update_all_authority_limits"
    )


def update_authority_limit(
    authority_limit: Any | None = None,
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """Update an existing authority limit.

    POST /api/v2/authority_limits/update_authority_limit
    """
    request_json: dict[str, Any] = {
        "authority_limit": _coerce_authority_limit_payload(authority_limit)
    }
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/authority_limits/update_authority_limit",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/authority_limits/update_authority_limit"
    )


__all__ = [
    "create_authority_limit",
    "delete_authority_limit",
    "get_authority_limit",
    "list_authority_limits",
    "update_all_authority_limits",
    "update_authority_limit",
]
