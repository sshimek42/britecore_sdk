"""BriteCore V2 claim_contacts API endpoint wrappers.

Manage contacts associated with claims including importing contacts
from policy records and maintaining claim-specific contact information.
"""

from __future__ import annotations

from typing import Any, Unpack

from britecore_sdk.api.api_calls import (
    BritecoreAPIClient,
    RequestParameters,
    api_client,
)

API_CLIENT: BritecoreAPIClient = api_client


def _coerce_claim_contact_payload(claim_contact: Any | None) -> Any | None:
    """Accept model instances and convert them to request-safe dict payloads."""
    if claim_contact is None:
        return None
    if hasattr(claim_contact, "to_dict") and callable(claim_contact.to_dict):
        return claim_contact.to_dict()
    return claim_contact


# --- Autogenerated spec wrappers ---


def create_claim_contact(
    claim_contact: Any | None = None,
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """Create a new contact associated with a claim.

    POST /api/v2/claim_contacts/create_claim_contact
    """
    request_json: dict[str, Any] = {
        "claim_contact": _coerce_claim_contact_payload(claim_contact)
    }
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/claim_contacts/create_claim_contact",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/claim_contacts/create_claim_contact"
    )


def delete_claim_contact(
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """Delete a claim contact record.

    POST /api/v2/claim_contacts/delete_claim_contact
    """
    request_json: dict[str, Any] = {}
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/claim_contacts/delete_claim_contact",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/claim_contacts/delete_claim_contact"
    )


def list_importable_contacts(
    revision_id: Any | None = None,
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """List contacts available for import from a policy revision.

    POST /api/v2/claim_contacts/list_importable_contacts
    """
    request_json: dict[str, Any] = {"revision_id": revision_id}
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/claim_contacts/list_importable_contacts",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/claim_contacts/list_importable_contacts"
    )


def update_claim_contact(
    claim_contact: Any | None = None,
    **kwargs: Unpack[RequestParameters],
) -> Any:
    """Update an existing claim contact record.

    POST /api/v2/claim_contacts/update_claim_contact
    """
    request_json: dict[str, Any] = {
        "claim_contact": _coerce_claim_contact_payload(claim_contact)
    }
    filtered_json = {k: v for k, v in request_json.items() if v is not None}
    request_result = API_CLIENT.do_request(
        path="/api/v2/claim_contacts/update_claim_contact",
        json=filtered_json,
        method="POST",
        **kwargs,
    )
    return API_CLIENT.process_result(
        request_result, endpoint="/api/v2/claim_contacts/update_claim_contact"
    )


__all__ = [
    "create_claim_contact",
    "delete_claim_contact",
    "list_importable_contacts",
    "update_claim_contact",
]
