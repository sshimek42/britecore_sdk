# Repository Structure Review — July 23, 2026

**Status:** Comprehensive audit complete
**Reviewer:** Automated code agent
**Focus Areas:** Layout organization, file placement, documentation management, cleanup opportunities

---

## Executive Summary

The repository has accumulated significant technical debt in its directory structure and root-level organization during active development. While the core codebase in `src/britecore_sdk/` follows proper conventions, the root directory contains:

- **6 Python maintenance scripts** that should be in `tools/`
- **20+ markdown files** that should be consolidated/archived
- **Documentation duplication** between root and `docs/` directories
- **Report and session files** that should be archived, not stored long-term

**Estimated effort to reorganize:** 2–4 hours
**Priority:** Medium (quality-of-life improvement, not blocking)

---

## SECTION 1: ROOT-LEVEL PYTHON SCRIPTS → MOVE TO `tools/`

### Current Issue
The following Python maintenance and generation scripts live at the repository root:

| Script | Purpose | Should Be | Status |
|--------|---------|-----------|--------|
| `batch_fix_remaining_docstrings.py` | Fix autogenerated docstrings in batch | `tools/batch_fix_docstrings.py` | One-time use |
| `count_remaining_wrappers.py` | Count remaining "Autogenerated wrapper" stubs | `tools/count_autogenerated_wrappers.py` | One-time use |
| `direct_fix.py` | Direct replacement of autogenerated stubs | `tools/direct_fix_stubs.py` | One-time use |
| `generate_docstrings.py` | Generate comprehensive docstrings | `tools/docstring_generator.py` | Utility |
| `generate_probe_regression_tests.py` | Generate tests from probe report | `tools/generate_probe_tests.py` | Utility (post-probe) |
| `update_wrappers_from_probe.py` | Update wrappers from probe discoveries | `tools/update_wrappers_from_probe.py` | Utility (post-probe) |

### Recommendation
Move all six scripts to `tools/` directory. These are developer utilities, not core SDK code or documentation.

### Implementation
```bash
# Move Python scripts to tools directory
Move-Item -Path "batch_fix_remaining_docstrings.py" -Destination "tools/"
Move-Item -Path "count_remaining_wrappers.py" -Destination "tools/"
Move-Item -Path "direct_fix.py" -Destination "tools/"
Move-Item -Path "generate_docstrings.py" -Destination "tools/"
Move-Item -Path "generate_probe_regression_tests.py" -Destination "tools/"
Move-Item -Path "update_wrappers_from_probe.py" -Destination "tools/"
```

**Post-move action:** Update any references in CI/scripts that invoke these tools with their new paths.

---

## SECTION 2: MARKDOWN FILES ORGANIZATION

### Current Issue: Documentation Duplication & Root Clutter

The root directory contains **20+ markdown files** mixed with:
- **Core user documentation**
- **Governance & legal docs**
- **Session reports and temporary work artifacts**
- **Strategic roadmaps and planning documents**

Additionally, the `docs/` directory has **100+ files**, many duplicating or overlapping with root content, with inconsistent naming (README.md vs readme.md, etc.).

### CATEGORY A: CORE DOCUMENTATION TO KEEP IN ROOT

**These are actively maintained and essential for users/developers:**

1. **README.md** — Project overview and quick start
2. **GETTING_STARTED.md** — Setup guide for new users
3. **API.md** — High-level API reference and domain overview
4. **ARCHITECTURE.md** — System design and components
5. **CONFIG_MANAGEMENT.md** — Configuration layers and management
6. **TROUBLESHOOTING.md** — Debugging and common issues
7. **AGENTS.md** — Developer workflow and repository conventions
8. **AGENTS.quickstart.md** — Compact developer reference
9. **BATCH_QUOTE_CREATION.md** — Domain-specific workflow guide
10. **PYTHON_COMPATIBILITY.md** — Version compatibility matrix (lightweight, canonical)

### CATEGORY B: GOVERNANCE & LEGAL DOCUMENTS TO KEEP IN ROOT

**Required for open-source/corporate projects:**

1. **CODE_OF_CONDUCT.md**
2. **CONTRIBUTING.md**
3. **SECURITY.md**
4. **DEPRECATION.md**
5. **LICENSING.md**
6. **STABILITY.md**
7. **CHANGELOG.md**
8. **LICENSE** (non-markdown, but required)
9. **ATTRIBUTION.md**

### CATEGORY C: FILES TO MOVE TO `docs/` (Create if needed)

**Strategic and implementation-focused documentation:**

1. **IMPROVEMENT_ROADMAP.md** → `docs/STRATEGIC_ROADMAP.md`
   - Keep as single source of truth for v2.x+ enhancements

2. **IMPLEMENTATION_CHECKLIST.md** → `docs/IMPLEMENTATION_CHECKLIST.md`
   - Tracks v2.0 feature parity

3. **ERROR_HINTS_IMPLEMENTATION.md** → `docs/examples/FIRST_IMPLEMENTATION_EXAMPLE.md`
   - First example of v2-specific feature (error hints)

4. **UNIMPLEMENTED_API_STUBS.md** → Keep in root (lightweight tracking file)

### CATEGORY D: FILES TO ARCHIVE IN NEW `archived_sessions/` DIRECTORY

**Work artifacts from development sessions — valuable history but should not clutter root:**

1. **FINAL_COMPREHENSIVE_COMPLETION_REPORT.md**
2. **SESSION_FINAL_COMPLETION_REPORT.md**
3. **MISSION_COMPLETE.md**
4. **PRIORITY_1_COMPLETION.md**
5. **ENDPOINT_DOCSTRING_VERIFICATION.md**
6. **ENDPOINT_FIXES_PROGRESS.md**
7. **ENDPOINT_FIXES_SESSION2_COMPLETE.md**
8. **DOCSTRING_AUDIT_SESSION_2026-07-20.md**
9. **DOCUMENTATION_CLEANUP_REPORT.md**
10. **DOCSTRING_IMPROVEMENT_BACKLOG.md**
11. **IMPLEMENTATION_SUMMARY.md** (move as IMPLEMENTATION_SUMMARY_2026-07-20.md)
12. **DELIVERY_REPORT.md** (consolidate into IMPROVEMENT_ROADMAP first)
13. **V2-PROGRESS-REPORT.md** (marked as archived)

### CATEGORY E: FILES TO CONSOLIDATE/MERGE

**These contain overlapping information and should be merged:**

| From | Into | Action |
|------|------|--------|
| IMPROVEMENTS_SUMMARY.md | IMPROVEMENT_ROADMAP.md | Merge summaries into roadmap |
| DELIVERY_REPORT.md | IMPROVEMENT_ROADMAP.md | Merge delivery status |
| INDEX.md | README.md or create docs/INDEX.md | Update navigation index |

### Directory Structure After Reorganization

```
britecore_sdk/
├── README.md (10 core user-facing docs here)
├── GETTING_STARTED.md
├── API.md
├── ARCHITECTURE.md
├── CONFIG_MANAGEMENT.md
├── TROUBLESHOOTING.md
├── AGENTS.md
├── AGENTS.quickstart.md
├── BATCH_QUOTE_CREATION.md
├── CHANGELOG.md
├── PYTHON_COMPATIBILITY.md
│
├── CODE_OF_CONDUCT.md (7 governance/legal docs)
├── CONTRIBUTING.md
├── SECURITY.md
├── DEPRECATION.md
├── LICENSING.md
├── STABILITY.md
├── LICENSE
├── ATTRIBUTION.md
│
├── docs/
│   ├── STRATEGIC_ROADMAP.md (moved from IMPROVEMENT_ROADMAP.md)
│   ├── IMPLEMENTATION_CHECKLIST.md
│   ├── examples/
│   │   └── FIRST_IMPLEMENTATION_EXAMPLE.md
│   └── ... (100+ API reference .rst files already here)
│
├── archived_sessions/
│   ├── FINAL_COMPREHENSIVE_COMPLETION_REPORT_2026-07-20.md
│   ├── SESSION_FINAL_COMPLETION_REPORT_2026-07-20.md
│   ├── MISSION_COMPLETE_2026-07-20.md
│   ├── ... (all other session reports)
│   └── README.md (explains purpose: "Historical work artifacts")
│
├── tools/
│   ├── batch_fix_docstrings.py
│   ├── count_autogenerated_wrappers.py
│   ├── direct_fix_stubs.py
│   ├── docstring_generator.py
│   ├── generate_probe_tests.py
│   ├── update_wrappers_from_probe.py
│   └── ... (existing check_wrapper_spec_mismatch.py, etc.)
│
├── src/britecore_sdk/ (active codebase — no changes)
├── tests/ (test suite — no changes)
├── examples/ (example scripts — no changes)
```

---

## SECTION 3: GENERATED DIRECTORIES (Already Correct)

### Current State: ✅ Proper gitignore

The following directories are **correctly ignored** in `.gitignore` and should not be committed:

- `.venv/` (virtual environment)
- `build/` (setuptools build output)
- `dist/` (distribution package output)
- `env/` (alternative venv)
- `.egg-info/` (package metadata)
- `htmlcov/` (coverage reports)
- `.pytest_cache/` (pytest cache)
- `.mypy_cache/` (type checker cache)
- `.ruff_cache/` (linter cache)
- `__pycache__/` (bytecode)

**Action:** None required. These are properly ignored.

---

## SECTION 4: `docs/` DIRECTORY DUPLICATION

### Current Issue: Inconsistent Documentation Duplication

The `docs/` folder contains Sphinx build artifacts and RST files (correct), BUT ALSO has:

- Duplicate markdown files: `readme.md`, `contributing.md`, `code_of_conduct.md`, etc. (lowercase versions)
- Overlap with root-level docs
- No clear separation between source docs and auto-generated reference

### Recommendation

1. **Remove all lowercase markdown duplicates** from `docs/` (they're not used by Sphinx build)
2. **Keep only `.rst` files** in `docs/` for Sphinx
3. **Keep root-level markdown** as source of truth for content
4. **Symlink or include root docs** in Sphinx `conf.py` if they're referenced in the build

---

## SECTION 5: REPORT JSON FILES

### Current Issue
Two report files exist at the root:
- `post_probe_report.json` (marked as non-source in `.gitignore`)
- `post_probe_report.md`

### Recommendation
These are **probe tool outputs** generated after running the API probe. They should be:

1. **Regenerated on each probe run** (not committed)
2. **Stored temporarily** in a `probe_output/` or `.probe_cache/` directory if needed
3. **Not relied upon for source control**

The `.gitignore` already excludes them — this is correct. Consider adding to `.gitignore`:
```gitignore
probe_output/
.probe_cache/
```

---

## SECTION 6: PROJECT CONFIGURATION FILES

### Current State: ✅ Well-Organized

The following configuration files are correctly placed at root:

| File | Purpose | Status |
|------|---------|--------|
| `pyproject.toml` | Package metadata & tool config (single source of truth) | ✅ Correct |
| `setup.py` | Legacy shim (correct use) | ✅ Correct |
| `pytest.ini` | Test configuration | ✅ Correct |
| `.pre-commit-config.yaml` | Pre-commit hooks | ✅ Correct |
| `.readthedocs.yml` | RTD build configuration | ✅ Correct |
| `.gitignore` | Git ignore rules | ✅ Correct |
| `.markdownlint.json` | Markdown linting rules | ✅ Correct |
| `.mypy.ini` (via `pyproject.toml`) | Type checking | ✅ Correct |

**Action:** None required.

---

## SECTION 7: HIDDEN CONFIG & CACHE DIRECTORIES

### Current State: ✅ Correct (Gitignored)

The following directories are properly ignored and can be cleaned if needed:

| Directory | Purpose | Action |
|-----------|---------|--------|
| `.idea/` | JetBrains IDE settings | Can be deleted locally (regenerated) |
| `.vscode/` | VSCode settings | Can be deleted locally (regenerated) |
| `.mypy_cache/` | Type checker cache | Can be deleted locally (regenerated) |
| `.ruff_cache/` | Linter cache | Can be deleted locally (regenerated) |
| `.pytest_cache/` | Pytest cache | Can be deleted locally (regenerated) |
| `.hypothesis/` | Hypothesis test data | Can be deleted locally (regenerated) |
| `__pycache__/` (throughout) | Python bytecode | Can be deleted locally (regenerated) |

**Action:** These can be cleaned with:
```bash
# Remove all caches
Remove-Item -Path ".mypy_cache" -Recurse -Force
Remove-Item -Path ".ruff_cache" -Recurse -Force
Remove-Item -Path ".pytest_cache" -Recurse -Force
Remove-Item -Path ".hypothesis" -Recurse -Force
Remove-Item -Path ".idea" -Recurse -Force -ErrorAction SilentlyContinue
Get-ChildItem -Path "." -Name "__pycache__" -Recurse -Directory | ForEach-Object { Remove-Item -Path $_ -Recurse -Force }
```

---

## SECTION 8: OTHER ROOT-LEVEL FILES TO REVIEW

### Files Worth Consolidating or Removing

| File | Category | Recommendation | Status |
|------|----------|-----------------|--------|
| `sphinx_build.log` | Build artifact | Remove | Can delete |
| `uv.lock` | Package lock (uv alternative) | Keep if uv is used, else remove | Review with team |
| `DOCS_AUDIT_REPORT.md` | Old audit report | Move to `archived_sessions/` | Archive |
| `post_probe_report.json` | Probe output | Regenerate on demand, don't commit | Already gitignored |
| `post_probe_report.md` | Probe output | Regenerate on demand, don't commit | Already gitignored |

---

## IMPLEMENTATION PLAN (Priority Order)

### Phase 1: Low-Risk Cleanup (30 minutes)
1. ✅ Delete cache directories (`.mypy_cache/`, `.ruff_cache/`, `.pytest_cache/`, `.hypothesis/`)
2. ✅ Remove `sphinx_build.log` if not actively used
3. ✅ Remove `post_probe_report.json` and `post_probe_report.md` (regenerated on probe)

### Phase 2: Core Reorganization (1 hour)
1. Create `archived_sessions/` directory with descriptive README
2. Move 13 session/report files to `archived_sessions/`
3. Consolidate `IMPROVEMENTS_SUMMARY.md` and `DELIVERY_REPORT.md` into `IMPROVEMENT_ROADMAP.md`
4. Move 6 Python scripts to `tools/`
5. Move 3 strategic docs to `docs/`

### Phase 3: Documentation Cleanup (30 minutes)
1. Remove lowercase markdown duplicates from `docs/`
2. Update `README.md` or create `docs/INDEX.md` with new structure navigation
3. Verify Sphinx build still works: `sphinx-build -W --keep-going -b html ./docs ./docs/_build/html-strict`

### Phase 4: Verification (15 minutes)
1. Run pre-commit hooks to ensure no regressions
2. Verify all imports and CI references still work
3. Commit all changes with clear message

### Estimated Total Effort: 2–3 hours

---

## RISKS & MITIGATIONS

| Risk | Mitigation |
|------|-----------|
| CI scripts reference old file paths | Update CI workflows (`.github/workflows/`) before moving files |
| Documentation links break | Search for internal links in markdown files before moving |
| Pre-commit hooks reference old paths | Update `.pre-commit-config.yaml` if needed |
| Team unfamiliar with changes | Add `archived_sessions/README.md` explaining structure |

---

## NOTES FOR FOLLOW-UP

1. **uv.lock**: Verify if `uv` package manager is actively used. If not, consider removing `uv.lock` and using `pip` exclusively. Check `pyproject.toml` for any uv-specific config.

2. **Version Control**: Some files like `uv.lock` and probe reports are marked as non-source in `.gitignore` but are currently visible. Ensure they're actually not tracked:
   ```bash
   git status
   git check-ignore uv.lock post_probe_report.json
   ```

3. **RTD Build**: After moving docs to `docs/` subdirectory, verify Sphinx still finds all includes in `.readthedocs.yml`.

4. **Compressed Sessions Archive**: Consider creating a compressed archive of the most important session reports (e.g., FINAL_COMPREHENSIVE_COMPLETION_REPORT_2026-07-20.md) for historical reference before deleting them.

---

## SUMMARY CHECKLIST

- [ ] Move 6 Python scripts to `tools/`
- [ ] Create `archived_sessions/` with 13 session reports
- [ ] Consolidate redundant markdown files
- [ ] Move strategic docs to `docs/`
- [ ] Remove lowercase markdown duplicates from `docs/`
- [ ] Delete cache directories
- [ ] Update README/INDEX navigation
- [ ] Verify Sphinx build
- [ ] Run pre-commit hooks
- [ ] Commit and verify CI

---

**Document prepared:** 2026-07-23
**Next review recommended:** After major version release or when accumulated artifacts reach 30+ files
