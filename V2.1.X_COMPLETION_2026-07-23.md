# v2.1.x Completion Update

*Date: July 23, 2026*

This update completes the current v2.1.x quality-of-life pass without starting any v3 work.

## What Was Completed

- Finalized the `quotes` wrapper quality pass in `src/britecore_sdk/api/api_calls/v2/quotes.py`.
- Upgraded the remaining short endpoint docstrings to full SDK-style docs (`Args`, `Returns`, `Raises`, endpoint note).
- Added missing required-parameter validation for key quote wrappers:
  - `associate_agentcy_to_quote`
  - `prefill_loss_history`
  - `prefill_violations`
  - `summary`
  - `turn_quote_into_application`
  - `update_e_delivery_enabled`

## Verification

- Source-level docstring check for `quotes.py` now reports **0 short docstrings**.
- Targeted unit test command executed:
  - `tests/unit/test_v2_endpoints.py::TestQuotesEndpoints`
  - `tests/unit/test_v2_coverage_gaps.py::TestPoliciesComplexEndpoints`
- Tests passed; local targeted run still shows global coverage gate failure (expected when running a subset only).

## Scope Statement

- This pass intentionally remains in **v2.1.x** scope.
- No v3 migrations, refactors, or API-surface changes were started.
